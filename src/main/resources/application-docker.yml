spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${POSTGRES_DB:fpvhub}
    username: ${POSTGRES_USER:fpvhub}
    password: ${POSTGRES_PASSWORD:fpvhub}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: redis
      port: 6379
